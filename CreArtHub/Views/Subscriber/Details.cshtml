@model CreArtHub.Domain.Entity.Subscriber

@{
    ViewData["Title"] = "Подробности";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@await Html.PartialAsync("_GetSubMenu",new List<string> { "Подписчики", ViewData["Title"].ToString() })

<section class="contact-us section bg-gray" id="contact">
    <div class="container">
        <h1>Подробности</h1>

        <div>
            <h4>Подписчик</h4>
            <hr />
            <dl class="row">
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.SubscribtionId)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.SubscribtionId)
                </dd>
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.User)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.User.Id)
                </dd>
            </dl>
        </div>
        @await Html.PartialAsync("_patternDetailsButtom",Model.Id)
    </div>
</section>
